import { ModuleHomePage } from '../../../components/tiles/ModuleHomePage';
import { rrhhTiles } from '../../../config/tiles';

export default function RRHHHomePage() {
  return <ModuleHomePage config={rrhhTiles} />;
}
